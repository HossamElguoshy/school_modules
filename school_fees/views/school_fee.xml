<odoo>

    <record id="view_school_fee_list" model="ir.ui.view">
        <field name="name">school.fee.list</field>
        <field name="model">school.fee</field>

        <field name="arch" type="xml">
            <list>

                <field name="student_id"/>
                <field name="total_amount"/>
                <field name="invoice_id"/>
                <field name="state"/>

            </list>
        </field>
    </record>


    <record id="view_school_fee_form" model="ir.ui.view">
        <field name="name">school.fee.form</field>
        <field name="model">school.fee</field>

        <field name="arch" type="xml">
            <form>

                <sheet>

                    <header>
                        <field name="state" widget="statusbar" statusbar_visible="draft,invoiced,paid,partial,overdue"/>

                        <button
                            name="action_create_invoice"
                            type="object"
                            string="Create Invoice"
                            class="btn-primary"
                           />
                        <button name="action_view_invoice" type="object" string="View Invoice" class="btn-primary"/>
                        <button name="action_update_state_from_invoice"
                            type="object"
                            string="Update State"/>

                    </header>

                    <group>

                        <field name="student_id"/>
                        <field name="total_amount" readonly="1"/>
                        <field name="invoice_id"/>

                    </group>

                    <notebook>

                        <page string="Fee Lines">

                            <field name="fee_line_ids">

                                <list editable="bottom">

                                    <field name="fee_structure_id"/>
                                    <field name="amount"/>

                                </list>

                            </field>

                        </page>

                    </notebook>

                 </sheet>



            </form>
        </field>
    </record>


    <record id="action_school_fee" model="ir.actions.act_window">
        <field name="name">Student Fees</field>
        <field name="res_model">school.fee</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>